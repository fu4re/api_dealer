export type ItemType = {
  id: number | string,
  displayname: string,
}

export type UserType = {
  id: number,
  avatar?: string | null,
  name?: string,
  surname?: string,
  lastname?: string,
  email?: string,
}
